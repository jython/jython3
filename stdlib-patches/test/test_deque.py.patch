--- lib-python/2.7/test/test_deque.py	2015-04-18 05:34:44.511111069 +0300
+++ Lib/test/test_deque.py	2015-04-18 05:34:31.827089833 +0300
@@ -646,6 +646,9 @@
         p = weakref.proxy(d)
         self.assertEqual(str(p), str(d))
         d = None
+        if test_support.is_jython:
+            from test_weakref import extra_collect
+            extra_collect()
         self.assertRaises(ReferenceError, str, p)
 
     def test_strange_subclass(self):
